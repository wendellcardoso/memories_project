import React from "react";

// import useStyles from "./styles";

const Auth = () => {
    // const classes = useStyles();
    const state = null;

    return (
        <div>
            AUTH
        </div>
    );
};

export default Auth;